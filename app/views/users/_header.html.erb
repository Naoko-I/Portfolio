<header class="navbar">
    <div class="nav navbar-left">
        <a class="app-logo" href="/">
          Bonheur Chocolat<%#= image_tag('logo.png') %>
        </a>
    </div>

    <div class="navber navbar-center">
      <div class="serch">
        <form class="form-inline my-2 my-lg-0">
           <input class="form-control mr-sm-3" type="text" placeholder="Search" aria-label="Search">
           <button class="glyphicon glyphicon-search" aria-hidden="true" type="submit"></button>
        </form>
      </div>
    </div>

   <% if user_signed_in? %>
      <ul class="nav navbar-nav navbar-right">
         <li>
            <%= link_to "", users_path, class: "glyphicon glyphicon-home fa-2x" %>
          </li>
          <li>
            <%= link_to "", user_favorites_path(current_user), class: "glyphicon glyphicon-heart-empty fa-2x" %>
          </li>
          <li>
            <%= link_to "", new_blog_path, class: "fa fa-paper-plane fa-2x" %>
          </li>
          <li>
            <%#linkつけるとhedaerからはみ出してしまうためコメントアウト中。。。= link_to user_path(current_user) do %>
            <%= attachment_image_tag current_user, :profile_image, size:'50x50', fallback: 'no_image.jpg', class: "img-circle pull-left profile-thumb" %>
            <%# end %>
          </li>
      </ul>
    <% end %>
</header>

