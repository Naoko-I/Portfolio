<div class="sidebar-right">
  <div class="sidebar-heading">
    <table class="current-user-profile">
      <tr>
        <th>
          <%= link_to user_path(current_user) do%>
            <%= attachment_image_tag current_user, :profile_image, size:'50x50', fallback: 'no_image.jpg', class: "profile-image-circle img-circle pull-left profile-thumb" %>
          <% end %>
        </th>
        <th>
          <%= link_to "#{current_user.name}", user_path(current_user) %>
        </th>
      </tr>
    </table>
  </div>
  <!-- following-user一覧 -->
  <div class="relationships-user-list">
    <div class="user-list-box">
      <div class="box-heading">
        <h5>フォロー中</h5>
        <h5><%= link_to "すべて見る", users_followings_path %></h5>
      </div>
      <table class="followed-user-list">
        <% @followeds.each do |followed| %>
          <tr>
            <td class="image">
              <%= link_to user_path(followed) do %>
              <%= attachment_image_tag followed.following, :profile_image, size:'45x45', format: 'jpeg', fallback: 'no_image.jpg', class: "img-circle pull-left profile-thumb" %>
              <% end %>
            </td>
            <td class="name"><%= followed.following.name %></td>
          </tr>
        <% end %>
      </table>
    </div>
  <!--  followed-user一覧 -->
    <div class="user-list-box">
      <div class ="box-heading">
        <h5>フォロワー</h5>
        <h5><%= link_to "すべて見る", users_followeds_path %></h5>
      </div>
      <table class="following-user-list">
        <% @followings.each do |following| %>
          <tr>
            <td class="image">
              <%= link_to user_path(following) do %>
              <%= attachment_image_tag following.followed, :profile_image, size:'40x40', fallback: 'no_image.jpg', class: "img-circle pull-left profile-thumb" %>
              <% end %>
            </td>
            <td class="name"><%= following.followed.name %></td>
          </tr>
        <% end %>
      </table>
    </div>
  </div>
</div>


