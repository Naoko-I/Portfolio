<body id="common-image">

<section class="slice slice-lg sct-color-1">
  <div class="container devise">
    <div class="row">
      <div class="col-md-4 col-md-offset-4">
        <div class="form-heading">
          	<h2>- Forgot your password? -</h2>
        </div>
      <!--入力フォーム  -->
        <div class="form-body">
          <!-- 登録ずみ名前、メールアドレス -->
              <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
              <%#= devise_error_messages! %>
                <div class="form-group has-icon">
                  <label class="control-label sr-only" for="inputEmail">Name</label>
                  <%= f.text_field :name, autofocus: true, class:"form-control", id:"inputName", placeholder:"ユーザネーム"%>
                </div>
                <div class="form-group has-icon">
                  <label class="control-label sr-only" for="inputEmail">Email</label>
                  <%= f.email_field :email, class:"form-control", id:"inputEmail", placeholder:"登録済みメールアドレス"%>
                </div>
              <% end %>

              <!-- パスワード再設定 -->
              <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
                <div class="form-group has-icon">
                  <label class="control-label sr-only" for="inputEmail">Password</label>
                  <%= f.password_field :password, class:"form-control", id:"inputPassword", placeholder:"パスワード(6文字以上)" %>
                </div>
                <div class="form-group has-icon">
                  <label class="control-label sr-only" for="inputEmail">Password</label>
                  <%= f.password_field :password_confirmation, autocomplete: "off", class:"form-control", id:"inputConfirmPassword", placeholder:"パスワード確認" %>
                </div>
                <%= f.submit "Change my password", class:"btn btn-block btn-base-1 mt-20" %>
              <% end %>

            <div class="form-footer">
              <%= render "devise/shared/links" %>
              <%= link_to "<< Back", root_path, class: "style-change" %>
            </div>
        </div>
      </div>
    </div>
  </div>
</section>

</body>

<!-- パスワード変更ページよりコピー　文字数判断で必要?
 -->
<%# if @minimum_password_length %>
  <!-- <em>(<%= @minimum_password_length %> characters minimum)</em><br /> -->
<%# end %>
